<% layout("/layouts/boilerplate") %>

  <div class="container mt-2">

    <style>
      #filters {
        display: flex;
        flex-wrap: nowrap;
        overflow-x: auto;
        gap: 1rem;
        scrollbar-width: none;
        align-items: center;
        padding-bottom: 0.25rem;
      }

      #filters::-webkit-scrollbar {
        display: none;
}


      .filter {
        text-align: center;
        margin: 1rem;
        margin-top: 0.2rem;
        margin-bottom: 0rem;
        min-width: 2px;
        opacity: 0.7;
        font-size: 0.8rem;
      }

      .filter:hover {
        opacity: 1;
        cursor: pointer;
      }

      .tax-info{
        display: none;
      }

      .tax-toggle {
        border: 1px solid black;
        border-radius: 0.8rem;
        height: 3rem;
        display: flex;
        align-items: center;
      }

      .filter p {
        margin-bottom: 0;
      }

      @media (max-width: 991.98px) {
        #filters {
          gap: 0.75rem;
        }

        .filter {
          margin: 0.5rem 0.35rem 0;
          font-size: 0.75rem;
        }

        .filter i {
          font-size: 0.95rem;
        }

        .tax-toggle {
          flex: 0 0 auto;
        }
      }

      @media (max-width: 575.98px) {
        #filters {
          gap: 0.5rem;
        }

        .filter {
          margin: 0.35rem 0.25rem 0;
          font-size: 0.7rem;
          min-width: 64px;
        }

        .filter i {
          font-size: 0.9rem;
        }

        .tax-toggle {
          width: 100%;
          justify-content: center;
          margin-top: 0.5rem;
        }
      }

    </style>

  <% if (!isWishlistPage) { %>
  <div id="filters">

    <a href="/listings?category=Trending" class="filter">
      <div class="filter">
        <div><i class="fa-solid fa-fire"></i></div>
        <p>Trending</p>
      </div>
    </a>
    <a href="/listings?category=Rooms" class="filter">
      <div class="filter">
        <div><i class="fa-solid fa-bed"></i></div>
        <p>Rooms</p>
      </div>
    </a>
    <a href="/listings?category=Iconic Cities" class="filter">
      <div class="filter">
        <div><i class="fa-solid fa-mountain-city"></i></div>
        <p>Iconic Cities</p>
      </div>
    </a>
    <a href="/listings?category=Mountains" class="filter">
      <div class="filter">
        <div><i class="fa-solid fa-mountain"></i></div>
        <p>Mountains</p>
      </div>
    </a>
    <a href="/listings?category=Castles" class="filter">
      <div class="filter">
        <div><i class="fa-brands fa-fort-awesome"></i></div>
        <p>Castles</p>
      </div>
    </a>
    <a href="/listings?category=Amazing Pools" class="filter">
      <div class="filter">
        <div><i class="fa-solid fa-person-swimming"></i></div>
        <p>Amazing Pools</p>
      </div>
    </a>
    <a href="/listings?category=Camping" class="filter">
      <div class="filter">
        <div><i class="fa-solid fa-campground"></i></div>
        <p>Camping</p>
      </div>
    </a>
    <a href="/listings?category=Farms" class="filter">
      <div class="filter">
        <div><i class="fa-brands fa-cow"></i></div>
        <p>Farms</p>
      </div>
    </a>
    <a href="/listings?category=Arctic" class="filter">
      <div class="filter">
        <div><i class="fa-solid fa-snowflake"></i></div>
        <p>Arctic</p>
      </div>
    </a>

      <!-- Used Bootstrap for Switching-->
      <div class="tax-toggle">
        <div class="form-check-reverse form-switch">
          <input class="form-check-input" type="checkbox" role="switch" id="switchCheckDefault">
          <label class="form-check-label" for="switchCheckDefault">Display total after taxes</label>
        </div>
      </div>

    </div>
    <% } %>

    <% if (isWishlistPage) { %>
      <div class="mt-3 mb-2 d-flex align-items-center gap-2">
        <h4 class="mb-0">My Wishlist</h4>
        <span class="badge text-bg-danger"><%= allListings.length %> saved</span>
      </div>
    <% } %>

    <% if (!isWishlistPage && (activeCategory || searchQuery)) { %>
      <div class="mt-3 mb-2">
        <% if (activeCategory) { %>
          <span class="badge text-bg-dark me-2">Category: <%= activeCategory %></span>
        <% } %>
        <% if (searchQuery) { %>
          <span class="badge text-bg-secondary">Search: <%= searchQuery %></span>
        <% } %>
      </div>
    <% } %>

    <% if (allListings.length === 0) { %>
      <div class="alert alert-warning mt-4" role="alert">
        <%= isWishlistPage ? "Your wishlist is empty." : "No listings found for your search." %>
      </div>
    <% } %>

    <div class="row row-cols-1 row-cols-sm-2 row-cols-lg-3 g-4 mt-3">

      <% for (let listing of allListings) { %>
        <% const isWishlisted = Array.isArray(wishlistIds) && wishlistIds.includes(String(listing._id)); %>

        <div class="card col listing-card h-100">
          <% if (currUser) { %>
            <form
              method="POST"
              action="<%= isWishlisted ? `/listings/${listing._id}/wishlist?_method=DELETE` : `/listings/${listing._id}/wishlist` %>"
              class="wishlist-form"
            >
              <button
                type="submit"
                class="wishlist-btn <%= isWishlisted ? 'active' : '' %>"
                aria-label="<%= isWishlisted ? 'Remove from wishlist' : 'Add to wishlist' %>"
              >
                <i class="<%= isWishlisted ? 'fa-solid' : 'fa-regular' %> fa-heart"></i>
              </button>
            </form>
          <% } %>

          <a href="/listings/<%= listing._id %>" class="listing-link">
            <img src="<%= listing.image.url %>" class="card-img-top listing-cover-img" alt="listing_image" />
            <!-- Image Overlays -->
            <div class="card-img-overlay"></div>
            <div class="card-body">
              <p class="card-text">
                <b>
                  <%= listing.title %>
                </b> <br />
                &#8377; <%= listing.price ? listing.price.toLocaleString("en-IN") : "N/A" %> / night
                <i class="tax-info"> &nbsp; &nbsp;+18%GST</i>
              </p>
            </div>
          </a>
        </div>
      <% } %>
    </div>

  </div>

  <script>
    let taxSwitch = document.getElementById("switchCheckDefault");
    if (taxSwitch) {
      taxSwitch.addEventListener("click",() => {
      let taxInfo = document.getElementsByClassName("tax-info");
      for(info of taxInfo) {
        if(info.style.display != "inline"){
          info.style.display="inline";
        }else {
          info.style.display = "none";
        }
        
      }
      });
    }
  </script>