<style>
  .search-btn {
    background-color: #fe424d;
    color: #fff;
    border-radius: 25px;
    padding: 0.5rem 1rem;
    
  }
  .search-btn:hover{
     background-color: #fe424d;
     color: #fff;
  }
  .search-btn i {
    display: inline;
    margin-right: 0.5rem;
  }
  .search-input {
    border-radius: 25px;
    padding: 0.5rem 1rem;
    font-size: 1rem;
    display: block;
  }
  .auth-links{
    display: flex;
    align-items: center;
    gap: 0.1rem;
}

.auth-links .nav-link{
    white-space: nowrap;
    width: max-content;
}
  .logout-form {
    margin: 0;
  }
  .logout-btn {
    background: transparent;
    border: 0;
    padding: 0.5rem 0;
    font-weight: 700;
  }

  .wishlist-nav i {
    color: #fe424d;
  }

  @media (max-width: 767.98px) {
    .auth-links {
      align-items: flex-start;
      width: 100%;
    }

    .auth-links .nav-link,
    .logout-btn {
      padding-top: 0.35rem;
      padding-bottom: 0.35rem;
    }
  }
</style>

<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/listings"><i class="fa-regular fa-compass"></i></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">

        <a class="nav-link" href="/listings">Explore</a>
      </div>

      <div class="navbar-nav ms-auto">
        <form class="search-form" role="search" action="/listings" method="GET">
          <input
            class="form-control me-2 search-input"
            type="search"
            name="search"
            value="<%= locals.searchQuery || '' %>"
            placeholder="Search destinations"
          />
          <button class="btn search-btn" type="submit"><i class="fa-solid fa-magnifying-glass"></i>Search</button>
        </form>
      </div>

      <div class="navbar-nav ms-auto auth-links">
        <a class="nav-link" href="/listings/new">Airbnb your home</a>
        <% if(!currUser) { %>
          <a class="nav-link " href="/signup"><b>Sign up</b></a>
          <a class="nav-link" href="/login"><b>Log in</b></a>
          <% } %>
            <% if(currUser) { %>
              <a class="nav-link wishlist-nav" href="/listings/wishlist"><i class="fa-solid fa-heart"></i> Wishlist</a>
              <a class="nav-link" href="/listings/orders">My Orders</a>
              <form class="logout-form" action="/logout" method="POST">
                <button class="nav-link logout-btn" type="submit">Log out</button>
              </form>
              <% } %>
      </div>


    </div>
  </div>
</nav>